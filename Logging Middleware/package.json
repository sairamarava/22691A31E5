{
  "name": "logging-middleware",
  "version": "1.0.0",
  "description": "A simple, reusable logging middleware package for full-stack URL shortener web applications. This package provides structured logging with automatic retry logic and context-rich information.",
  "main": "simple-test.js",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "keywords": [],
  "dependencies": {
    "axios": "^1.10.0",
    "express": "^5.1.0"
  }
}
